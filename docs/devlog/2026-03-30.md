# 开发日记 2026-03-30

| 类别 | 内容 |
|------|------|
| **产品思路** | 开发日志改为表格格式：产品思路 / 遇到的坑 / 进度 / 其他，更结构化 |
| **进度** | Coach 对话多 Provider 跑通：Gemini 主力，Claude 备用，自动 fallback |
| **坑** | `google-genai==1.10.0` 版本不存在，Railway 装包失败导致 Gemini 从未运行，一直 fallback 到 Claude。解决：用 `pip show google-genai` 确认本地版本 1.47.0，更新 requirements.txt |
| **坑** | Gemini API 要求对话历史必须以 user 角色开头，否则报错。Coach 历史第一条是教练开场白（assistant），导致每次发消息 Gemini 失败。解决：发送前过滤掉开头的 assistant 消息 |
| **坑** | `model_used` 变量只在生成开场白时赋值，已有历史消息的对话打开时变量未定义报 NameError。解决：在函数开头初始化为 None |
| **坑** | 写 requirements.txt 版本号凭感觉，没验证是否存在。教训：写版本前先 `pip show 包名` 确认 |
| **其他** | 用户 API key 意外在对话中暴露，立即提醒重新生成 |
