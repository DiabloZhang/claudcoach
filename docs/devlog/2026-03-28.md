# 开发日记 2026-03-28

## 今天做了什么

**Railway 云端部署收尾**
- 修复 `strava_id` 超出 PostgreSQL Integer 范围的问题（改为 BigInteger）
- 修复 PostgreSQL 表结构与 SQLite 不兼容问题（drop + recreate tables）
- 修复 FRONTEND_URL 缺少 `https://` 导致 OAuth 跳转拼错路径
- Strava 授权 + 数据同步 + TSS 计算全部在云端跑通

**Dashboard 功能增强**
- 体能趋势图高度可调（矮 420px / 中 840px / 高 1680px / 超高 2520px）
- 立即同步按钮（触发 sync → 等5秒 → calculate-tss → 刷新数据）
- 体能图悬浮 Tooltip：显示当日 CTL/ATL/TSB + 训练名称/时长/TSS
- 最近训练按天分组，每天右侧气泡图（游泳→骑行→跑步，大小=TSS）

**异常数据管理系统**
- `Activity` 新增 `tss_adjusted` 字段：异常时存 0，未来可修正为估算值
- 同步流程自动检测异常：新活动入库时即时判断，异常直接标记 + `tss_adjusted=0`
- CTL/ATL/TSB 计算改用有效 TSS：`is_excluded` 时用 `tss_adjusted`（而非直接跳过）
- `/analysis/anomalies/{user_id}/backfill`：历史数据一键补扫接口
- 前端「异常」标签：活动名旁显示红色「异常」tag，TSS 不计入气泡和统计

**PLAN.md 新增「临时需求」section**
用于记录不属于主线 Phase 的细节优化需求，避免污染主线规划。

## 确立的工作哲学

**能不依靠用户就不依靠用户**

起因：我没有线上数据库的访问权，无法查到 2026-03-14 异常游泳的活动 ID。
错误做法：让用户手动查 ID，再手动调用排除接口。
正确做法：做一个 `/backfill` 接口，自动扫全量数据、自动排除、自动补字段；同时在同步流程里加入自动检测，以后新活动永不遗漏。

**判断标准**：
- 让用户"去找某个数据"→ 先想能不能写接口自动完成
- 让用户"手动执行某步骤"→ 先想能不能集成进现有流程
- 只有真正需要用户决策时才打断用户

这个哲学已记入 `CLAUDE.md`，下次对话自动加载。

## 遇到的问题

- PostgreSQL `Integer` 只支持到 ~21亿，Strava activity ID 已超过，需用 `BigInteger`
- PostgreSQL 不支持 SQLite 的 `PRAGMA table_info`，迁移逻辑需分别处理
- Railway PORT 变量会被平台自动覆盖，不能手动设置 PORT=3000（前端实际跑在 8080）

**同步系统升级**
- 两个同步按钮：立即同步（增量，从最新活动时间起）+ 同步指定日期（手选起点）
- 新增 `SyncLog` 表：记录每次同步时间、起点、新增/跳过数量、Strava API 调用次数、耗时、状态
- 新增「同步记录」tab：表格展示所有同步历史
- 立即同步按钮自动执行完整流程：sync → backfill → calculate-tss → 刷新
- TSS/小时 > 100 显示「阈值偏低?」黄色警告标签

## Phase 2a 完成确认

Railway 云端部署全部跑通，包括：
- 后端 / 前端 / PostgreSQL 三服务稳定运行
- Strava OAuth + 数据同步 + TSS 计算
- 异常数据自动检测 + 标记
- 同步历史追踪

## 下一步

进入 Phase 2b（分析深化）或 Phase 3（AI 教练），由用户决定方向。
